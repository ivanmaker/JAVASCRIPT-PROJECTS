/*Declare and assign two variables for later use*/
var sent1 = "This is the beginning of the string", sent2 = " and this is the end of the string"; 

/*Concatenate the two previously created variable and display them in a popup*/
window.alert(sent1 + sent2);